The Assignment2.ipynb can be loaded on Google Colab and can be run as is. The data loaded in the file is loaded via data.gov.sg API (see Load Dataset section in the ipynb file). 

The code was optimised using Cluade AI for faster run and chat log of the same is available in file “Code Optimization for Faster VAE Training on CPU Without GPU Usage.pdf”
